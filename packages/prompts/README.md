# Revival prompts

Nemotron prompts live here as versioned, reviewable source files. Each model call records
the prompt version and environment-based model alias used so evaluation results remain
reproducible.
