packages:
  - apps/*
  - packages/*

# pnpm 11 blocks dependency build scripts unless they are approved here, and
# `strictDepBuilds` (on by default) fails the install while any are pending.
# esbuild's install script fetches the platform binary that `vite build` needs.
allowBuilds:
  esbuild: true
